<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="review"> 

<select id="list" resultType="Review">
	SELECT rvImg, userEmail, rvContent FROM tbl_rv where pNo = #{pNo}
</select>

<insert id="add">
	INSERT INTO tbl_rv (pNo, userNo, rvDate, rvImg, rvContent) VALUES (#{pNo}, #{userNo}, SYSDATE(),  #{rvImg}, #{rvContent})
</insert>


</mapper>